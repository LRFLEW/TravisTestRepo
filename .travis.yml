language: c++
script:
  - echo TESTING

matrix:
  include:
    - os: linux
      sudo: required
      dist: trusty
      before_install:
        - sudo dpkg --add-architecture i386
        - sudo apt-get update -qq
      install:
        - sudo apt-get install -y gcc-multilib zlib1g-dev:i386
